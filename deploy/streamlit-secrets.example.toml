# Copy these lines into Streamlit Community Cloud → App → Settings → Secrets
# Replace YOUR_API_URL with your deployed backend (no trailing slash)

API_BASE_URL = "https://YOUR_API_URL"
CHAT_ENDPOINT_URL = "https://YOUR_API_URL/chat/answer"
